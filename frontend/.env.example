VITE_API_URL=http://localhost:4000/api

# Optional: enables the "Continue with Google" button. Must match the
# backend's GOOGLE_CLIENT_ID (same OAuth 2.0 Web application client).
VITE_GOOGLE_CLIENT_ID=
